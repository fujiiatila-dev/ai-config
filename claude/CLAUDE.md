# Instruções globais do Claude Code

O padrão de trabalho é o mesmo em qualquer ferramenta. Ele vive em `WORKFLOW.md`
e é importado abaixo — não duplique regras aqui.

@WORKFLOW.md

Antes de sincronizar esta configuração, rode `./install.sh --validate` (ou
`.\install.ps1 --validate`) e revise o `--dry-run`; a instalação repete o
preflight antes de escrever no perfil local.

@RTK.md
